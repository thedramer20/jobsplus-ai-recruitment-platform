<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jobplus.mapper.MessageMapper">

    <resultMap id="msgResultMap" type="com.jobplus.model.Message">
        <id     property="id"             column="id"/>
        <result property="conversationId" column="conversation_id"/>
        <result property="senderId"       column="sender_id"/>
        <result property="content"        column="content"/>
        <result property="readAt"         column="read_at"/>
        <result property="createdAt"      column="created_at"/>
    </resultMap>

    <select id="findByConversationId" resultMap="msgResultMap">
        SELECT * FROM message WHERE conversation_id = #{conversationId}
        ORDER BY created_at ASC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO message (conversation_id, sender_id, content)
        VALUES (#{conversationId}, #{senderId}, #{content})
    </insert>

    <update id="markRead">
        UPDATE message SET read_at = NOW()
        WHERE conversation_id = #{conversationId}
          AND sender_id != #{userId}
          AND read_at IS NULL
    </update>

    <select id="countUnread" resultType="int">
        SELECT COUNT(*) FROM message
        WHERE conversation_id = #{conversationId}
          AND sender_id != #{userId}
          AND read_at IS NULL
    </select>

</mapper>
