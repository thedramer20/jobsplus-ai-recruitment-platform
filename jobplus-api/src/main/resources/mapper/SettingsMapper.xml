<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jobplus.mapper.SettingsMapper">

    <resultMap id="userSettingsResultMap" type="UserSettings">
        <id     property="userId"       column="user_id"/>
        <result property="settingsJson" column="settings_json"/>
        <result property="updatedAt"    column="updated_at"/>
    </resultMap>

    <select id="findByUserId" resultMap="userSettingsResultMap">
        SELECT user_id, settings_json, updated_at
        FROM user_settings
        WHERE user_id = #{userId}
    </select>

    <insert id="upsert">
        INSERT INTO user_settings (user_id, settings_json)
        VALUES (#{userId}, #{settingsJson})
        ON DUPLICATE KEY UPDATE settings_json = #{settingsJson}, updated_at = CURRENT_TIMESTAMP
    </insert>

</mapper>
