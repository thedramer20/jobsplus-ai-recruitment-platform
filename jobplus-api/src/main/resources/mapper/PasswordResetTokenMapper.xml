<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jobplus.mapper.PasswordResetTokenMapper">

    <resultMap id="tokenResultMap" type="PasswordResetToken">
        <id     property="id"        column="id"/>
        <result property="userId"    column="user_id"/>
        <result property="tokenHash" column="token_hash"/>
        <result property="expiresAt" column="expires_at"/>
        <result property="used"      column="used"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO password_reset_token (user_id, token_hash, expires_at, used)
        VALUES (#{userId}, #{tokenHash}, #{expiresAt}, COALESCE(#{used}, FALSE))
    </insert>

    <select id="findByTokenHash" resultMap="tokenResultMap">
        SELECT id, user_id, token_hash, expires_at, used, created_at
        FROM password_reset_token
        WHERE token_hash = #{tokenHash}
    </select>

    <update id="markUsed">
        UPDATE password_reset_token
        SET used = TRUE
        WHERE id = #{id}
    </update>

    <update id="invalidateAllForUser">
        UPDATE password_reset_token
        SET used = TRUE
        WHERE user_id = #{userId} AND used = FALSE
    </update>

</mapper>
